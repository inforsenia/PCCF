PDFS/
temp/
temp_*/
plantilles_*/
boe/temp-txt.txt
tools/__pycache__/
luatex.*/
memories_*
# (sense barra final: memories_ESOBAT/memories_FP ara poden ser symlinks
# a la carpeta sincronitzada d'OneDrive, i "memories_*/" no els cobria)
*.bak
*.kate-swp

# Xarxa de seguretat: el token d'onedrive NO ha d'estar mai dins del repositori
# (repositori públic). Viu sempre en un volum Docker extern, mai ací.
refresh_token
onedrive_confdir/
.config/onedrive/
